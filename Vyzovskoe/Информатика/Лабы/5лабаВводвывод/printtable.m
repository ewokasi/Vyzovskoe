function res = printtable (x, y, z)
%нарисовать таблицу из двух входныъ векторов
space = 13;

fprintf("/--------------------------------------\\ \n") %шапка таблицы
fprintf("| Аргумент |     Функция |     Интеграл|\n")
fprintf("|--------------------------------------|\n")

for i= 1:1:length(x)                      %середина таблицы 
    fprintf('|%7.3f   |\t%7.3f  |%12.4f  |\n', x(i), y(i),z(i))
end
fprintf('\\--------------------------------------/\n'); %хвост таблицы

end